global:
  scrape_interval: 15s # Как часто Prometheus будет опрашивать приложения.
scrape_configs:
  - job_name: 'myapp' # Имя задания (произвольное, для идентификации).
    static_configs:
      - targets: ['host.docker.internal:8000']